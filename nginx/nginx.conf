events {}

http {

    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    upstream django {
        server web:8000;
    }

    server {
        listen 80;
        server_name ullas-jobtracker.duckdns.org;

        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name ullas-jobtracker.duckdns.org;

        ssl_certificate /etc/letsencrypt/live/ullas-jobtracker.duckdns.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/ullas-jobtracker.duckdns.org/privkey.pem;

        location / {
            proxy_pass http://django;

            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
        }

        location /static/ {
            alias /app/staticfiles/;
        }
    }
}